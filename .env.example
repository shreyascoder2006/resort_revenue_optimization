# Optional. When set, the AI concierge (src/lib/engines/concierge-llm.ts) calls the
# real Claude API for grounded, conversational replies. Without it, the concierge
# transparently falls back to the deterministic rule-based engine in concierge.ts -
# the app works either way.
ANTHROPIC_API_KEY=
