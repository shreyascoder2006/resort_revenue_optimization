# Both optional. When set, the AI concierge calls a real LLM for grounded,
# conversational replies - tried in this order: Gemini, then Claude, then the
# deterministic rule-based engine in concierge.ts. The app works with neither set.
GEMINI_API_KEY=your_gemini_api_key_here

